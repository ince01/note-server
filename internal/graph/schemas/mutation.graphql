type Mutation {
  noteCreate(note: NoteInput!): Note! @isAuthenticated
  noteUpdate(note: NoteInput!): Note! @isAuthenticated

  userCreate(user: UserInput!): User!

  tokenCreate(userCredential: UserCredential!): Token!
}
